info:
  name: Game config
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/game-config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get the live config for the game.

  **Authentication**
  - Required scope: `read:gameConfig`.
