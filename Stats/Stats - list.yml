info:
  name: Stats - list
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/game-stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get all game stats.

  **Authentication**
  - Required scope: `read:gameStats`.
