info:
  name: Stats - all for player
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/game-stats/player-stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get the current values of all the player's stats.

  **Authentication**
  - Required scope: `read:gameStats`.
  - Authenticated by: `X-Talo-Alias`.
