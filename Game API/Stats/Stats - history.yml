info:
  name: Stats - history
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/v1/game-stats/:statInternalName/history"
  params:
    - name: page
      value: "0"
      type: query
    - name: startDate
      value: ""
      type: query
      disabled: true
    - name: endDate
      value: ""
      type: query
      disabled: true
    - name: statInternalName
      value: "{{statInternalName}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get a history of changes to a player stat.

  **Authentication**
  - Required scope: `read:gameStats`.
  - Authenticated by: `X-Talo-Player`.

  **Path**
  - `{{statInternalName}}` - The internal name of the stat.

  **Query**
  - `page` (optional, default `0`) - The current pagination index.
  - `startDate` (optional) - UTC date (YYYY-MM-DD), ISO 8601 datetime, or millisecond timestamp.
  - `endDate` (optional) - Same format as `startDate`.
